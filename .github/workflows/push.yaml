name: "test listener"
# Controls when the workflow will run

on:
  push:
    
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
      - name: test
        run: ls -la
